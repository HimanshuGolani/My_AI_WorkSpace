# Sub-Agent: DB Optimizer

I fire automatically when DB schemas, queries, or migrations are involved.

## Checks
Missing indexes · N+1 JPA patterns · SELECT * usage · unpaginated queries · unbounded transactions · Flyway/Liquibase correctness · schema naming

## Output: Missing indexes, query issues, generated migration SQL file